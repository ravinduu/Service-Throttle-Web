import React from "react";
import { StyleSheet, Text, View } from "react-native";

const AccountScreen = () => {
  return (
    <View>
      <Text>Im Account bitc</Text>
    </View>
  );
};

export default AccountScreen;

const styles = StyleSheet.create({});
